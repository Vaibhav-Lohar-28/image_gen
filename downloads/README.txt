Put model files you already have into this folder, mirroring ComfyUI's models/ layout, e.g.:

  downloads/diffusion_models/Krea 2/Your models/Animosity_Krea2_V1.0_int8_convrot.safetensors

Everything here is merged into /comfyui/models at image build time (see Dockerfile, stage 3).
